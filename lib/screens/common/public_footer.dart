
import 'package:flutter/material.dart';

class PublicFooter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 50,
      child: Center(
        child: Text('Public Footer'),
      ),
    );
  }
}
